<Window x:Class="VRisingServerManager.ModManager"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VRisingServerManager"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        mc:Ignorable="d"
        Title="Mod管理器" Height="710" Width="1100" ResizeMode="NoResize">

    <Window.Resources>
        <local:VersionToolTipConverter x:Key="VersionToolTipConverter"/>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </Window.Resources>

    <StackPanel Orientation="Vertical">
        <ui:CommandBar>
            <ui:AppBarButton Label="刷新Mod列表" Icon="Refresh" Click="RefreshModlistButton_Click" ToolTip="刷新Mods列表。"/>
            <ui:AppBarButton Label="安装BepInEx" Icon="Add" Click="InstallBepInExButton_Click" DataContext="{Binding SelectedItem, ElementName=ServerComboBox}" ToolTip="在选定的服务器上安装BepInEx，这是Mods运行所必需的。">
                <ui:AppBarButton.Style>
                    <Style TargetType="ui:AppBarButton">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding BepInExInstalled}" Value="True">
                                <Setter Property="IsEnabled" Value="False"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ui:AppBarButton.Style>
            </ui:AppBarButton>

            <ui:AppBarButton Label="卸载BepInEx" Icon="Remove" Click="UninstallBepInExButton_Click" DataContext="{Binding SelectedItem, ElementName=ServerComboBox}" ToolTip="从选定的服务器中卸载BepInEx。">
                <ui:AppBarButton.Style>
                    <Style TargetType="ui:AppBarButton">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding BepInExInstalled}" Value="False">
                                <Setter Property="IsEnabled" Value="False"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ui:AppBarButton.Style>
            </ui:AppBarButton>

            <ui:CommandBar.Content>
                <DockPanel>
                    <TextBlock Text="服务器：" VerticalAlignment="Center" Padding="10,0,0,0"/>
                    <ComboBox Name="ServerComboBox" ItemsSource="{Binding Servers}" DisplayMemberPath="vsmServerName" VerticalAlignment="Center" Width="Auto" Margin="5" SelectionChanged="ServerComboBox_SelectionChanged" ToolTip="选择要管理的服务器。"/>
                    <TextBlock VerticalAlignment="Center" DataContext="{Binding SelectedItem, ElementName=ServerComboBox}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding BepInExInstalled}" Value="True">
                                        <Setter Property="Text" Value="BepInEx：已安装："/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding BepInExInstalled}" Value="False">
                                        <Setter Property="Text" Value="BepInEx：未安装"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Text="{Binding BepInExVersion}" VerticalAlignment="Center" DataContext="{Binding SelectedItem, ElementName=ServerComboBox}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding BepInExInstalled}" Value="False">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding BepInExInstalled}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <TextBlock Text="  搜索：" VerticalAlignment="Center" Margin="20,0,0,0"/>
                    <TextBox x:Name="ModSearchBox" 
                            Width="250" 
                            VerticalAlignment="Center" 
                            Margin="5,0,20,0"
                            TextChanged="ModSearchBox_TextChanged"
                            ToolTip="输入Mod名称搜索">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Padding" Value="5"/>
                                <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
                                <Setter Property="BorderThickness" Value="2"/>
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Background="{TemplateBinding Background}" 
                                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="10">           
                                                <DockPanel>
                                                    <ui:SymbolIcon Symbol="Find" DockPanel.Dock="Right" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                                    <ScrollViewer x:Name="PART_ContentHost" 
                                                                    Margin="5,0,5,0" 
                                                                    VerticalAlignment="Center"/>
                                                </DockPanel>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </DockPanel>
            </ui:CommandBar.Content>
        </ui:CommandBar>

        <StackPanel Orientation="Horizontal" Width="Auto">
            <DataGrid x:Name="ModsDataGrid" 
                      Width="600" 
                      Height="600"
                      CanUserResizeColumns="False"
                      CanUserResizeRows="False"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      CanUserDeleteRows="False" 
                      CanUserReorderColumns="False" 
                      SelectionChanged="ModsDataGrid_SelectionChanged" 
                      SelectionMode="Single" 
                      RowHeaderWidth="0">
                <DataGrid.Resources>
                    <ContextMenu x:Key="myContextMenu">
                        <MenuItem Header="打开Mod文件夹" Click="ModDirectory_Click"/>
                        <MenuItem Header="Mod配置文件修改器" Click="ModConfigEditor_Click">
                            <MenuItem.Icon>
                                <ui:SymbolIcon Symbol="Edit"/>
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                    <Style TargetType="DataGridCell">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mod名称" Width="313" Binding="{Binding Name}" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="Padding" Value="5 5 0 0"/>
                                <Setter Property="ContextMenu" Value="{StaticResource myContextMenu}"/>
                                <Setter Property="ToolTip" Value="{Binding ToolTipText}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <DataGridTemplateColumn Header="版本">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Versions[0].Version_Number}" Margin="0,0,5,0" Padding="5 5 0 0"/>
                                    <!--<TextBlock Text="{Binding LocalVersion}" Visibility="{Binding LocalVersion, Converter={StaticResource StringToVisibilityConverter}}" Foreground="Gray" FontSize="10" VerticalAlignment="Center"/>-->
                                    <TextBlock Text="↑" Foreground="Red" Visibility="{Binding UpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}}" ToolTip="有可用更新"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridCheckBoxColumn Header="已下载" Width="85" Binding="{Binding Downloaded, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True">
                        <DataGridCheckBoxColumn.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridCheckBoxColumn.CellStyle>
                    </DataGridCheckBoxColumn>
                    <DataGridCheckBoxColumn Header="已安装" Width="85" Binding="{Binding Installed, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True">
                        <DataGridCheckBoxColumn.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridCheckBoxColumn.CellStyle>
                    </DataGridCheckBoxColumn>
                </DataGrid.Columns>
            </DataGrid>

            <GroupBox Header="Mod信息" Padding="5" Margin="5" Width="490" Height="600">
                <ScrollViewer>
                    <StackPanel x:Name="ModInfoPanel" DataContext="{Binding SelectedItem, ElementName=ModsDataGrid}">
                        <Image Name="ModImage" HorizontalAlignment="Center" Stretch="Fill" Width="128" Height="128" Margin="0,0,0,10"/>
                        <DockPanel>
                            <StackPanel>
                                <DockPanel>
                                    <TextBlock Text="作者：" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Owner}"/>
                                </DockPanel>
                                <DockPanel>
                                    <TextBlock Text="创建日期：" FontWeight="Bold" Margin="0,10,0,0"/>
                                    <TextBlock Text="{Binding Date_Created}" Margin="0,10,0,0"/>
                                </DockPanel>
                                <DockPanel>
                                    <TextBlock Text="最新更新：" FontWeight="Bold" Margin="0,10,0,0"/>
                                    <TextBlock Text="{Binding Date_Updated}" Margin="0,10,0,0"/>
                                </DockPanel>
                                <DockPanel>
                                    <TextBlock Text="下载次数：" FontWeight="Bold" Margin="0,10,0,0"/>
                                    <TextBlock Name="DownloadsTextBlock" Margin="0,10,0,0"/>
                                </DockPanel>
                                <DockPanel>
                                    <TextBlock Text="Mod下载页面：" FontWeight="Bold" Margin="0,10,0,0"/>
                                    <TextBlock Margin="0,10,0,0">
                                        <Hyperlink NavigateUri="{Binding Package_Url}" RequestNavigate="Hyperlink_RequestNavigate">
                                            点击跳转
                                        </Hyperlink>
                                    </TextBlock>
                                </DockPanel>
                            </StackPanel>
                            <StackPanel Margin="10,0,0,0">
                                <DockPanel>
                                    <TextBlock Text="版本：" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Versions[0].Version_Number}"/>
                                </DockPanel>
                                <DockPanel>
                                    <TextBlock Text="本地版本：" FontWeight="Bold" Margin="0,10,0,0"/>
                                    <TextBlock Text="{Binding LocalVersion}" Margin="0,10,0,0"/>
                                </DockPanel>
                                <DockPanel>
                                    <TextBlock Text="版本更新：" FontWeight="Bold" Margin="0,10,0,0"/>
                                    <TextBlock Text="更新可用！" FontWeight="Bold" Foreground="Red" Margin="0,10,0,0" Visibility="{Binding UpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                    <!--<TextBlock Text="暂无更新" FontWeight="Bold" Foreground="Gray" Margin="0,10,0,0" Visibility="{Binding UpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}}"/>-->
                                </DockPanel>
                                <DockPanel>
                                    <TextBlock Text="文件大小：" FontWeight="Bold" Margin="0,10,0,0"/>
                                    <TextBlock Name="FileSizeTextBlock" Margin="0,10,0,0"/>
                                </DockPanel>
                                <DockPanel>
                                    <TextBlock Text="Mod源码网站：" FontWeight="Bold" Margin="0,10,0,0"/>
                                    <TextBlock Margin="0,10,0,0">
                                        <Hyperlink NavigateUri="{Binding Versions[0].Website_Url}" RequestNavigate="Hyperlink_RequestNavigate">
                                            点击跳转
                                        </Hyperlink>
                                    </TextBlock>
                                </DockPanel>
                            </StackPanel>
                        </DockPanel>
                        <DockPanel>
                            <TextBlock Text="依赖：" FontWeight="Bold" Margin="0,10,0,0"/>
                            <TextBlock Name="DependenciesTextBlock" TextWrapping="Wrap" Margin="0,10,0,0"/>
                        </DockPanel>
                        <DockPanel>
                            <TextBlock Text="类型：" FontWeight="Bold" Margin="0,10,0,0"/>
                            <TextBlock Name="CategoriesTextBlock" TextWrapping="Wrap" Margin="0,10,0,0"/>
                        </DockPanel>
                        <DockPanel>
                            <TextBlock Text="描述：" FontWeight="Bold" Margin="0,10,0,0"/>
                            <TextBlock Text="{Binding Versions[0].Description}" TextWrapping="Wrap" Margin="0,10,0,0"/>
                        </DockPanel>
                        <StackPanel Margin="0,20,0,0" HorizontalAlignment="Center">
                            <DockPanel>
                                <Button Width="125" Click="DownloadButton_Click" ToolTip="下载所选的Mod。">
                                    <Button.Style>
                                        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Downloaded}" Value="False">
                                                    <Setter Property="IsEnabled" Value="True"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Downloaded}" Value="True">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                    <Setter Property="Foreground" Value="Gray"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <DockPanel>
                                        <ui:SymbolIcon Symbol="Download"/>
                                        <TextBlock Text="下载" Margin="5,0,0,0"/>
                                    </DockPanel>
                                </Button>
                                <Button Width="125" Margin="10,0,0,0" Click="InstallButton_Click" ToolTip="将选定的Mod安装到选定的服务器。">
                                    <Button.Style>
                                        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Downloaded}" Value="True">
                                                    <Setter Property="IsEnabled" Value="True"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Downloaded}" Value="False">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                    <Setter Property="Foreground" Value="Gray"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Installed}" Value="True">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                    <Setter Property="Foreground" Value="Gray"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <DockPanel>
                                        <ui:SymbolIcon Symbol="Add"/>
                                        <TextBlock Text="安装" Margin="5,0,0,0"/>
                                    </DockPanel>
                                </Button>
                                <Button Width="125" Margin="10,0,0,0" Click="UpdateModButton_Click" ToolTip="更新所选Mod">
                                    <Button.Style>
                                        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding UpdateAvailable}" Value="True">
                                                    <Setter Property="IsEnabled" Value="True"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding UpdateAvailable}" Value="False">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                    <Setter Property="Foreground" Value="Gray"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <DockPanel>
                                        <ui:SymbolIcon Symbol="Add"/>
                                        <TextBlock Text="更新" Margin="5,0,0,0"/>
                                    </DockPanel>
                                </Button>
                            </DockPanel>
                            <DockPanel Margin="0,10,0,10">
                                <Button Width="125" Click="RemoveButton_Click" ToolTip="删除选定的Mod存档。">
                                    <Button.Style>
                                        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Downloaded}" Value="True">
                                                    <Setter Property="IsEnabled" Value="True"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Downloaded}" Value="False">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                    <Setter Property="Foreground" Value="Gray"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <DockPanel>
                                        <ui:SymbolIcon Symbol="Delete"/>
                                        <TextBlock Text="移除" Margin="5,0,0,0"/>
                                    </DockPanel>
                                </Button>
                                <Button Width="125" Margin="10,0,0,0" Click="UninstallButton_click" ToolTip="从选定的服务器上卸载选定的Mod。">
                                    <Button.Style>
                                        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Installed}" Value="True">
                                                    <Setter Property="IsEnabled" Value="True"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Installed}" Value="False">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                    <Setter Property="Foreground" Value="Gray"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <DockPanel>
                                        <ui:SymbolIcon Symbol="Remove"/>
                                        <TextBlock Text="卸载" Margin="5,0,0,0"/>
                                    </DockPanel>
                                </Button>
                            </DockPanel>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </GroupBox>
        </StackPanel>
        <StatusBar>
            <StatusBarItem>
                <ui:ProgressBar Name="DownloadProgressBar" IsIndeterminate="True" Height="10" Width="800" Visibility="Hidden" Margin="10,0,0,0"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Name="DownloadProgressText" Margin="10,0,0,0"/>
            </StatusBarItem>
        </StatusBar>
    </StackPanel>
</Window>    